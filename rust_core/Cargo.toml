# rust_core/Cargo.toml
[package]
name = "singularity_rust"
version = "1.0.0"
edition = "2024"

[lib]
crate-type =["staticlib"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = 3
lto = true
codegen-units = 1
debug = true
strip = "none"